# Rust Web Documentation

- Keep route, service contract, extractor, and integration docs aligned with the codebase.
- Document async cancellation safety, `unsafe` invariants, and error propagation strategy when they are non-obvious.
- Use generated inventories or deterministic scans before writing free-form summaries.
- Update the canonical module doc when a handler, service trait, or repository method changes its contract.
