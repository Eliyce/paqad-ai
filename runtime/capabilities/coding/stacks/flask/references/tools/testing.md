# Flask Testing

- Default test command: `pytest`
- Prefer application-factory fixtures and request client coverage over direct view-function testing.
- Cover auth, blueprint registration, validation, and serialization behavior.
