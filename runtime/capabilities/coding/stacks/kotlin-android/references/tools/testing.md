# Kotlin Android Testing

- Default test command: `./gradlew test`
- Add instrumentation or compose UI coverage for critical navigation and auth flows.
- Cover Room, Retrofit, and ViewModel behavior independently from screen rendering.
