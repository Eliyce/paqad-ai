# ASP.NET Core Testing

- Default test command: `dotnet test`
- Prefer xUnit or NUnit with `WebApplicationFactory` for integration coverage.
- Cover controller, minimal API, and authorization policy behavior with request-level tests.
