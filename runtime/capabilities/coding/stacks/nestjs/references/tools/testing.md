# NestJS Testing

- Default test command: `pnpm test`
- Cover controllers and providers with unit tests, then add e2e coverage for guards, validation, and transport adapters.
- Include GraphQL or message-pattern tests when those traits are active.
