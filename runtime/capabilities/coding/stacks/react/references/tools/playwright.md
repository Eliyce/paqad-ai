# React Playwright Guide

Use Playwright for non-destructive browser validation of running React flows.

## Good Uses

- route transitions and guard behavior
- auth and permission-blocked journeys
- browser-visible error leakage and fallback boundaries
- critical happy-path and negative-path UI flows
