# React Code Quality Guide

Use the project's defined package scripts first. If scripts are unclear, inspect `package.json` before inventing a command flow.

## Priorities

- component responsibility boundaries
- route-level data fetching and error handling
- typing of props, loaders, and server responses
- stale UI, route, and error documentation
