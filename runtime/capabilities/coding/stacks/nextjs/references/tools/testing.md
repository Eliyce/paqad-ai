# Next.js Testing

- Default test command: `pnpm test` or your package-manager equivalent.
- Cover App Router pages, route handlers, middleware, and authenticated server actions.
- Prefer request-level tests for `app/api` and component tests for route UI boundaries.
