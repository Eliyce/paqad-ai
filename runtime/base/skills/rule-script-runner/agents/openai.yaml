interface:
  display_name: Rule Script Runner
  short_description: Runs registered rule scripts diff-scoped during
    feature-development.checks, hash-caches the findings report, blocks the
    stage on deterministic findings under mode:strict, and routes heuristic
    findings to review without blocking.
