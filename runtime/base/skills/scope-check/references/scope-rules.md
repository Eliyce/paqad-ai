# Scope Rules

- `within-scope`: request is already described by the active spec artifacts
- `extension-needed`: request is related but adds new behavior or constraints
- `blocked-no-spec`: implementation-class work has no active spec artifact
